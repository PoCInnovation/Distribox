#cloud-config

hostname: distribox
fqdn: distribox.local

users:
  - name: user
    groups: sudo
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    lock_passwd: true
ssh_pwauth: false

package_update: true
package_upgrade: true
